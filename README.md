# COMP-432 ML Workspace

## Setup

```bash
mise trust && mise install && mise run install
```

## Running Notebooks

* **In VS Code:** Open any `.ipynb` file. The editor will auto-detect the local `.venv`.
* **In the browser (JupyterLab):** Run `jupyter lab` in your terminal. `mise` automatically handles the environment activation.

